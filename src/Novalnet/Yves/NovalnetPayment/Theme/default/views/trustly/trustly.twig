{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">        
        <p>{{ 'novalnet.trustly-payment-description' | trans }}</p>
        {{ form_errors(data.form.novalnetTrustly) }}
        {{ form_widget(data.form.novalnetTrustly) }}
    </div>
{% endblock %}
